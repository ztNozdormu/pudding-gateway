spring:
  application:
    name: pudding-gateway
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: local
  cloud:
      nacos:
        discovery:
          serverAddr: 47.107.170.59:8848
#  cloud:
#    config:
#      profile: local
#      label: master

---

spring:
  profiles: local
#  cloud:
#    config:
#      enabled: true
#      uri: http://127.0.0.1:8002
server:
  port: 9091

---

spring:
  profiles: dev
  cloud:
    config:
      uri: http://127.0.0.1:8002

---

spring:
  profiles: test
  cloud:
    config:
      uri: http://127.0.0.1:8002

---

spring:
  profiles: produce
  cloud:
    config:
      uri: http://127.0.0.1:8002